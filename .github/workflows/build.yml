name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    name: Build & test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Test, validate config, and package
        run: ./gradlew --no-daemon test verifyPluginProjectConfiguration buildPlugin

      - name: Upload distribution
        uses: actions/upload-artifact@v4
        with:
          name: logtime-plugin
          path: build/distributions/*.zip